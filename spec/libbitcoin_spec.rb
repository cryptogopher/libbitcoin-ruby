require 'spec_helper'

describe Libbitcoin do
  it 'has a version number' do
    expect(Libbitcoin::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
